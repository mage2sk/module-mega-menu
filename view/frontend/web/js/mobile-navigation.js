/**
 * Panth MegaMenu - Mobile Navigation (Accordion Mode)
 *
 * Disabled - Mobile menu now uses accordion pattern instead of slide navigation
 * The accordion functionality is implemented directly in the menu template
 */
define(['jquery'], function($) {
    'use strict';

    return function(config, element) {
        // Accordion mode - no additional JavaScript needed
        // All functionality is in the template's inline script
    };
});
